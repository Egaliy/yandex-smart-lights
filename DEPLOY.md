# 🚀 Инструкция по деплою на GitHub Pages

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий:
   - **Repository name**: `yandex-smart-lights` (или любое другое название)
   - **Visibility**: Public (для бесплатного GitHub Pages) или Private
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

## Шаг 2: Загрузка кода

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Веб-интерфейс для управления умными лампочками"

# Добавление remote репозитория (замените на ваш URL)
git remote add origin https://github.com/Egaliy/yandex-smart-lights.git

# Отправка кода
git branch -M main
git push -u origin main
```

## Шаг 3: Настройка GitHub Pages

### Вариант A: Автоматический деплой (рекомендуется)

1. Перейдите в **Settings** вашего репозитория
2. В левом меню выберите **Pages**
3. В разделе **Source** выберите **GitHub Actions**
4. Готово! При каждом push в ветку `main` приложение автоматически задеплоится

### Вариант B: Ручной деплой

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source**:
   - Выберите ветку: `main` (или `master`)
   - Выберите папку: `/ (root)`
3. Нажмите **Save**
4. Через несколько минут приложение будет доступно по адресу:
   ```
   https://egaliy.github.io/yandex-smart-lights/
   ```

## Шаг 4: Настройка OAuth приложения

⚠️ **ВАЖНО**: После деплоя нужно обновить Redirect URI в OAuth приложении!

1. Перейдите на https://oauth.yandex.ru/
2. Откройте ваше приложение
3. Добавьте Redirect URI:
   ```
   https://egaliy.github.io/yandex-smart-lights/
   ```
4. Сохраните изменения

## Шаг 5: Проверка работы

1. Откройте ваш сайт: `https://egaliy.github.io/yandex-smart-lights/`
2. Введите Client ID
3. Нажмите "Войти через Яндекс"
4. Авторизуйтесь
5. Управляйте лампочками! 💡

## Обновление приложения

После внесения изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически задеплоит обновления (если настроен автоматический деплой).

## Устранение проблем

### Приложение не открывается
- Проверьте, что в Settings → Pages выбрана правильная ветка
- Убедитесь, что все файлы загружены в репозиторий
- Подождите несколько минут после первого деплоя

### OAuth не работает
- Проверьте, что Redirect URI в OAuth приложении точно совпадает с URL GitHub Pages
- Убедитесь, что URL начинается с `https://` (не `http://`)
- Проверьте права доступа в OAuth приложении (`iot:view` и `iot:control`)

### Устройства не загружаются
- Проверьте, что токен действителен
- Убедитесь, что устройства подключены к Яндекс Умному Дому
- Откройте консоль браузера (F12) для просмотра ошибок


