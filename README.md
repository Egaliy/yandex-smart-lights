# 💡 Управление умными лампочками через API Яндекс Умного Дома

Веб-приложение для управления умными лампочками через Яндекс Умный Дом с красивым визуальным интерфейсом.

## 🌐 Веб-интерфейс

### Демо на GitHub Pages

После настройки GitHub Pages приложение будет доступно по адресу:
```
https://ваш-username.github.io/умные-лампочки/
```

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-username/умные-лампочки.git
cd умные-лампочки
```

2. Откройте `index.html` в браузере или используйте локальный сервер:
```bash
# Python
python -m http.server 8000

# Node.js
npx serve
```

3. Откройте в браузере: `http://localhost:8000`

## 🚀 Настройка GitHub Pages

### Автоматический деплой

1. Создайте репозиторий на GitHub
2. Загрузите код в репозиторий
3. Перейдите в **Settings** → **Pages**
4. В разделе **Source** выберите **GitHub Actions**
5. При следующем push в ветку `main` или `master` приложение автоматически задеплоится

### Ручной деплой

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите ветку (например, `main`)
3. Выберите папку `/ (root)`
4. Нажмите **Save**

## 🔐 Настройка OAuth для веб-интерфейса

### Важно для GitHub Pages

При создании OAuth приложения на [oauth.yandex.ru](https://oauth.yandex.ru/client/new):

1. **Redirect URI** должен быть:
   - Для GitHub Pages: `https://ваш-username.github.io/умные-лампочки/`
   - Для локальной разработки: `http://localhost:8000/` или `http://127.0.0.1:8000/`

2. Можно добавить несколько Redirect URI (для локальной разработки и продакшена)

3. Отметьте права:
   - ✅ `iot:view` - просмотр устройств
   - ✅ `iot:control` - управление устройствами

## 📱 Возможности веб-интерфейса

- ✅ **Авторизация через OAuth Яндекс** - безопасный вход
- ✅ **Визуальный пульт управления** - красивые карточки устройств
- ✅ **Включение/выключение** - переключатель одним кликом
- ✅ **Регулировка яркости** - плавный слайдер
- ✅ **Управление цветом** - выбор RGB цвета
- ✅ **Температура цвета** - теплый/нейтральный/холодный свет
- ✅ **Адаптивный дизайн** - работает на всех устройствах
- ✅ **Темная тема** - приятный для глаз интерфейс

## 🐍 Python скрипт (CLI)

Для работы из командной строки:

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Использование

### Способ 1: Через переменную окружения

```bash
export YANDEX_SMART_HOME_TOKEN='ваш_токен_доступа'
python yandex_smart_home_api.py
```

### Способ 2: Ввод токена при запуске

```bash
python yandex_smart_home_api.py
```

Скрипт запросит токен доступа.

## Как получить токен доступа

Для работы с API Яндекс Умного Дома нужно получить OAuth токен:

### Шаг 1: Регистрация приложения

1. Перейдите на [OAuth Яндекс](https://oauth.yandex.ru/client/new)
2. Создайте новое приложение:
   - **Название**: любое (например, "Управление лампочками")
   - **Платформа**: выберите "Веб-сервисы"
   - **Redirect URI**: укажите любой URL (например, `https://google.com`)
3. В разделе **"Доступ к данным"** отметьте права:
   - ✅ `iot:view` - просмотр устройств
   - ✅ `iot:control` - управление устройствами
4. Нажмите "Создать" и **сохраните CLIENT_ID**

### Шаг 2: Получение токена

1. Сформируйте URL (замените `ВАШ_CLIENT_ID` на ваш ID):
   ```
   https://oauth.yandex.ru/authorize?response_type=token&client_id=ВАШ_CLIENT_ID
   ```

2. Откройте этот URL в браузере и авторизуйтесь в Яндекс

3. После авторизации вы будете перенаправлены на Redirect URI с токеном в URL:
   ```
   https://google.com#access_token=ВАШ_ТОКЕН&token_type=bearer&expires_in=31536000
   ```

4. Скопируйте значение `access_token` из URL - это ваш токен доступа

### Шаг 3: Использование токена

```bash
export YANDEX_SMART_HOME_TOKEN='ваш_токен'
python yandex_smart_home_api.py
```

Подробная документация: https://yandex.ru/dev/dialogs/smart-home/doc/ru/concepts/platform-quickstart

## Возможности

- ✅ Получение списка всех устройств
- ✅ Просмотр информации об устройствах
- ✅ Просмотр возможностей (capabilities) каждого устройства
  - Включение/выключение
  - Яркость
  - Цвет (RGB)
  - Температура цвета
- ✅ Просмотр свойств (properties) устройств
- ✅ Сохранение списка устройств в JSON файл
- ✅ Фильтрация и подсчет лампочек

## Пример использования

```python
from yandex_smart_home_api import YandexSmartHomeAPI

# Инициализация с токеном
api = YandexSmartHomeAPI(access_token='ваш_токен')

# Получить список всех устройств
devices = api.get_devices()

# Вывести список в консоль
api.list_devices(save_to_file=True)  # сохранить в JSON

# Получить информацию о конкретном устройстве
device = api.get_device_info('device_id')
```

